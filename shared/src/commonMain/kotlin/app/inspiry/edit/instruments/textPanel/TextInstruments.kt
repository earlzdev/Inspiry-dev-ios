package app.inspiry.edit.instruments.textPanel

enum class TextInstruments(val index: Int) {
    TEXT_ANIMATION(0),
    TEXT_COLOR(1),
    TEXT_BACKGROUND(2),
    TEXT_FONT(3),
    TEXT_SIZE(4),
    TEXT_ALIGNMENT(5),
    TEXT_LAYERS(6)
}